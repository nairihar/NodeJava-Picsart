function createStack() {
  let values = [];


  return {
    hello: 'text',
    push: function(value) {
      values.push(1);
    },

    pop: function() {
      values[]
    }
  }
}

let newInstance = createStack();

newInstance // {}